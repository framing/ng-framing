export interface FramingRootComponentConfig {
  /**
   * Set to true to add the root component to entryComponents instead of bootstrap for AngularJS / Angular hybrid mode.
   */
  hybrid?: boolean;
}
